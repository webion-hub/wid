services:
  kaire: 
    is_production: yes
    daemon_address: http://192.168.1.194:8080
    site_id: 1
    app_path: /webion/TestIIS
    
    delete_directory: true
    build_command: dotnet publish -o out
    bundle_dir: out/Webion.IIS.Daemon
    log_dir: logs